const MOBILE = 480;
const TABLET = 768;
const SMALL_DEVICES = 1024;
const DESKTOP = 1280;

export const $mobileWidth = `${MOBILE}px`;
export const $tabletWidth = `${TABLET}px`;
export const $smallDevices = `${SMALL_DEVICES}px`;
export const $desktopWidth = `${DESKTOP}px`;